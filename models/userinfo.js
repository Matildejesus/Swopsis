class UserInfo {
  constructor(userName, email, password, group, profilePicture, coins, totalCarbon, totalLitres, totalWeight) {
    this.userName = userName;
    this.email = email;
    this.password = password;
    this.group = group;
    this.profilePicture = profilePicture;
    this.coins = coins;
    this.totalWeight = totalWeight;
    this.totalLitres = totalLitres;
    this.totalCarbon = totalCarbon;
    this.itemsSwapped = itemsSwapped;
  }
}

export default UserInfo;
